import { ModulePlaceholderPage } from '@shared/components/primitives';

export const TalentPlaceholderPage = (): JSX.Element => {
  return <ModulePlaceholderPage namespace="talent" placeholderKey="talent:page.placeholder" />;
};
