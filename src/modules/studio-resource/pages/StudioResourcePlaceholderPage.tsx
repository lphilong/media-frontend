import { ModulePlaceholderPage } from '@shared/components/primitives';

export const StudioResourcePlaceholderPage = (): JSX.Element => {
  return (
    <ModulePlaceholderPage
      namespace="studio-resource"
      placeholderKey="studio-resource:page.placeholder"
    />
  );
};
