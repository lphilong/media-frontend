import { ModulePlaceholderPage } from '@shared/components/primitives';

export const WorkSchedulePlaceholderPage = (): JSX.Element => {
  return (
    <ModulePlaceholderPage
      namespace="work-schedule"
      placeholderKey="work-schedule:page.placeholder"
    />
  );
};
