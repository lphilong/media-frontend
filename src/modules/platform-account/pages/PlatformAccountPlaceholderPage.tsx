import { ModulePlaceholderPage } from '@shared/components/primitives';

export const PlatformAccountPlaceholderPage = (): JSX.Element => {
  return (
    <ModulePlaceholderPage
      namespace="platform-account"
      placeholderKey="platform-account:page.placeholder"
    />
  );
};
