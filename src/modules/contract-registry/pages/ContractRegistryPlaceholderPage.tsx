import { ModulePlaceholderPage } from '@shared/components/primitives';

export const ContractRegistryPlaceholderPage = (): JSX.Element => {
  return (
    <ModulePlaceholderPage
      namespace="contract-registry"
      placeholderKey="contract-registry:page.placeholder"
    />
  );
};
